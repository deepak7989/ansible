- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item.name }}"
      state: present
    loop:
    - { name: "nginx", state: "present" }
    - { name: "mysql", state: "absent" }
    - { name: "zip", state: "present" }